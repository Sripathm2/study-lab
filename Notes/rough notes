learning with decay means the step size decreases as we get to hiugher epochs

character langiuage model whyerew it treste each input as an example, it models the sequence of characters and leanring how to predict the next character

what is broadcasting semantics in this we make copies of a vectoir to match size of a vectro to a matrix then we do element wise opertation like division.,


log likelyhood is the sum of the log of the propabilites of each event 

we use negative log likelyghood becuase we want when a loss fucntion is lower that means we are doing better but loh likelyhood is opposite.

we can then use normlaized (average ) likelyhood for cleaner version.

@ is matrix multiplication operator in pytroch 

log counts  is logits 

the confusing part is my log likelyhood taken as a count and whay logits taken as a alternative to count. this probablity part is softmax 
'
foprward pass only is like prediction backpropagation using a loss function is the training part.

 we can do regularization to force a nmodel to have close to 0 weights and control how much regularization should wee use.